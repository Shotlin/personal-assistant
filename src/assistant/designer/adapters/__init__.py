"""Designer source adapters: authorized normalization of external resources.

Open WebUI remains the authoring owner of its prompts/skills/knowledge
(R07); this package only lists, reads and writes through the user's own
authorized upstream API — never through database access.
"""
