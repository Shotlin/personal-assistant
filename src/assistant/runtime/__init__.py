"""Guarded local action runtime (master plan WP5+ modules live here).

Currently: trusted desktop session ownership (WP3). Local recipes, the
strict router, and the compact planner are added by their work packages.
"""
