[package]
name = "sani"
version = "0.1.0"
description = "Sani — local desktop voice shell for the Personal Assistant Deep Agent"
authors = ["Sayan"]
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[target.'cfg(target_os = "macos")'.build-dependencies]
cc = "1.4"

[dependencies]
tauri = { version = "2", features = ["macos-private-api", "tray-icon"] }
tauri-plugin-global-shortcut = "2"
tauri-plugin-autostart = "2"
cpal = "0.15"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "io-util", "process"] }
futures-util = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
uuid = { version = "1", features = ["v4"] }
parking_lot = "0.12"
log = "0.4"
env_logger = "0.11"
window-vibrancy = "0.5"

# Dev-only WebView inspector: `npm run tauri -- dev --features devtools`
# (then SANI_DEVTOOLS=pill,panel opens it on window creation).
[features]
devtools = ["tauri/devtools"]

[profile.release]
strip = true
lto = true
codegen-units = 1
